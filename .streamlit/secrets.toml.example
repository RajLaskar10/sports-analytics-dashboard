# Copy this file to .streamlit/secrets.toml and fill in your Supabase credentials.
# secrets.toml is gitignored — never commit it.
#
# When deploying to Streamlit Community Cloud, paste these values into the
# Secrets section of your app settings (no file needed).

[supabase]
host = "db.<your-project-ref>.supabase.co"
port = 5432
database = "postgres"
user = "postgres"
password = "<your-supabase-db-password>"
